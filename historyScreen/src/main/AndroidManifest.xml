<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="ru.cocovella.historyscreen">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_historyscreen">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name="ru.cocovella.history_screen.history.HistoryActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>